rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /schools/{document=**} {
      allow read, create: if true;
    }
    match /debateState/{state} {
      allow read, write: if true;
    }
  }
}
